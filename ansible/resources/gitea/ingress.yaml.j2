---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dash-cert
  namespace: gitea
spec:
  commonName: 'gitea.{{ dns_domain }}'
  dnsNames:
    - gitea.{{ dns_domain }}
  issuerRef:
    name: ipa
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 4096
  secretName: dash-cert
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  namespace: gitea 
  name: rook-gui
  annotations:
    external-dns.alpha.kubernetes.io/target: dash.{{ dns_domain }}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`gitea.{{ dns_domain }}`)
      kind: Rule
      services:
        - name: gitea-http
          port: 3000
  tls:
    secretName: dash-cert
